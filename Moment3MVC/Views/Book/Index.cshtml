@model IEnumerable<Moment3MVC.Models.Book>

@{
    ViewData["Title"] = "Books";
}

<h1 class="title">Books List</h1>

<p>
    <a class="button is-primary" asp-controller="Book" asp-action="Create">Create New</a>
</p>

<table class="table is-striped is-hoverable">
    <thead>
        <tr>
            <th>Title</th>
            <th>Author</th>
            <th>Published Date</th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @if (Model == null)
        {
            <tr>
                <td colspan="4">No books found</td>
            </tr>
        }
        else
        {
            @foreach (var item in Model)
            {
                <tr>
                    <td>@item.Title</td>
                    <td>@item.Author</td>
                    <td>@item.PublishedDate.ToShortDateString()</td>
                    <td>
                        <a class="button is-small is-info" asp-controller="Book" asp-action="Update" asp-route-id="@item.Id">Edit</a>
                        <!--Kind of scuffed delete, its a form with the base valude delete to call the function and gets the id of the current item, but it works-->
                        <form asp-controller="Book" asp-action="Delete" asp-route-id="@item.Id" method="post" style="display:inline;">
                            <button type="submit" class="button is-small is-danger">Delete</button>
                        </form>
                        <a class="button is-small is-link" asp-controller="Book" asp-action="Details" asp-route-id="@item.Id">Details</a>
                    </td>
                </tr>
            }
        }
    </tbody>
</table>